{
  "name": "chessable2pgn",
  "version": "1.0.0",
  "description": "Parse a chessable text into pgn",
  "main": "dist/app.js",
  "scripts": {
    "start": "tsc && node dist/app.js",
    "debug": "tsc && node --inspect --inspect-brk dist/app.js ",
    "test": "jest --coverage",
    "test:watch": "jest --coverage --watchAll",
    "build": "tsc",
    "build:watch": "onchange 'src/**/*.ts' -- npm run build",
    "view:coverage": "serve coverage/lcov-report"
  },
  "keywords": [],
  "author": "Stephan Hesse",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^27.0.1",
    "@types/node": "^16.6.2",
    "jest": "^27.0.6",
    "ts-jest": "^27.0.5",
    "typescript": "^4.3.5"
  },
  "dependencies": {
    "yargs": "^16.2.0"
  }
}
